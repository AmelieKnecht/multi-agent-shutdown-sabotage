# Running the experiments (run_experiment.py, run_grid.py) needs only these two.
openai==2.21.0
httpx==0.28.1

# Reproducing the statistics in the Results section additionally needs:
pandas==3.0.1
numpy==2.4.2
scipy==1.17.1          # boschloo_exact
statsmodels==0.14.6    # Holm-Bonferroni, Wilson intervals

# The figures (and the shared helpers the statistics import from the plot_*
# modules: short_name, wilson_ci, count_arm, newcombe_diff_ci) additionally need:
matplotlib==3.10.8
SciencePlots==2.2.2
